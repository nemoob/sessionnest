# CHANGELOG

### v0.2.48(20260723)
#### quality:
1. 增加仅使用合成 JSONL 的公开 Token 扫描性能基准，以 2 万个累计检查点和 5 秒上限拦截数量级性能退化，并纳入全量回归
2. 普通浏览状态不再自动持久化自由文本搜索词；已删除的保存视图、标签、分类或项目会安全回退到最近会话
3. 批量归档与取消归档增加代际保护，过期异步结果不会覆盖更新后的列表和选择状态
4. 脱敏诊断与截图共用事务式剪贴板写入，复制失败时恢复原有多类型内容
5. Token 扫描改为线性分块拆行，避免超长 compacted 日志反复扫描导致启动后长期占满 CPU；弹框隐藏时同步停止刷新图标动画

### v0.2.47(20260723)
#### quality:
1. 现有元数据 SQLite 首次进入版本化 schema 迁移前原子保留一次性备份，迁移失败会回滚原库且不会覆盖首次备份

### v0.2.46(20260723)
#### optimization:
1. 常驻菜单栏期间在下一次 24 小时到期点自动检查 GitHub Release；关闭自动检查会取消定时器，重新开启后按持久化的上次检查时间恢复

### v0.2.45(20260723)
#### feature:
1. 会话管理器支持用 ⌘F 聚焦搜索，Esc 先清空搜索再退出焦点；VoiceOver 将会话行整体读出并提供打开与收藏动作

### v0.2.44(20260723)
#### bugFix:
1. 状态栏截图写入系统剪贴板失败时恢复原有内容；无法完整备份原内容时不清空剪贴板，避免复制失败造成数据丢失

### v0.2.43(20260723)
#### optimization:
1. 会话管理器在本机恢复上次侧栏范围、时间、排序和项目目录展开状态，重新启动后可继续原有浏览位置；普通搜索词不自动持久化

### v0.2.42(20260723)
#### feature:
1. 会话列表支持原生多选并批量收藏、取消收藏、归档和取消归档；批量归档部分失败时保留成功结果与失败项选择，便于直接重试

### v0.2.41(20260723)
#### feature:
1. 会话搜索支持用空格分隔多个关键词并跨标题、预览、项目、分支和标签同时命中，列表内可见命中词使用强调色高亮

### v0.2.40(20260723)
#### feature:
1. 可将当前会话范围、搜索词、时间和排序保存为侧栏视图，一键恢复组合筛选；保存视图独立持久化并可安全删除

### v0.2.39(20260723)
#### feature:
1. 侧栏按至少两个项目共享的真实父目录自动生成可筛选的智能文件夹，聚合子树会话计数；单项目不增加冗余路径层级

### v0.2.38(20260723)
#### feature:
1. 统计页支持单击项目 Token 排行按精确目录下钻会话明细，显示当前项目与会话数并可一键恢复全部会话

### v0.2.37(20260723)
#### feature:
1. 统计页新增最近 90 天预设和本地自然日自定义闭区间；历史区间保留区间内有 Token 但后来继续更新的会话，并复用最近一次自定义统计快照

### v0.2.36(20260723)
#### bugFix:
1. 状态栏每日 Token 图表按服务端周额度周期起点生成本地日期轴，周期从非零点开始时保留首个部分日；周期不可用时继续回退最近 7 天

### v0.2.35(20260723)
#### optimization:
1. 状态栏弹框统一采用更舒适的 440pt 宽度并显式展示纵向滚动条；每日 Token 首次有数据时默认选中最新一天，数据更新与用户清除选择后不强制重置

### v0.2.34(20260723)
#### optimization:
1. 状态项以周额度剩余为主信息、以本额度周期本地 Token 为次信息；弹框首屏优先展示总 Token 与非缓存 Token，右键菜单和无障碍文本同步使用完整口径

### v0.2.33(20260723)
#### optimization:
1. 额度刷新按主窗口或菜单栏弹框的前后台状态与 macOS 低电量状态动态调整唤醒频率；回到前台时仅补齐已过期数据，状态切换会替换旧定时计划，避免后台固定十分钟空唤醒

### v0.2.32(20260723)
#### optimization:
1. 将 7 天、30 天和全部时间统计快照持久化到 SessionNest SQLite；仅在应用版本、自然日、时区、会话、有效项目归属、覆盖归属与每日 Token 的 SHA-256 输入键完全一致时复用，过期或损坏快照自动回退现算

### v0.2.31(20260723)
#### optimization:
1. 额度周期 Token 查询复用秒级时间索引，并在 SQLite 内按会话与本地自然日汇总后返回，减少刷新时传入 Swift 的明细行和重复聚合

### v0.2.30(20260723)
#### optimization:
1. Token 扫描启动前用 150 毫秒尾沿窗口合并短时间重复请求，只执行最新参数对应的目标发现与扫描；已启动扫描继续按代际安全替换

### v0.2.29(20260723)
#### optimization:
1. 使用原生 FSEvents 感知 Codex 会话日志和状态库变化，无变化时不再按时间重复完整扫描；事件仅标记待刷新，监听不可用时保留原定时回退

### v0.2.28(20260723)
#### feature:
1. 状态栏扫描诊断支持一键复制稳定纯文本报告，仅包含版本、系统与时区、聚合覆盖原因、异常状态和扫描计数，不接收账号、会话标识、目录路径或日志内容

### v0.2.27(20260723)
#### feature:
1. 统计页支持按本地自然日范围从原始 JSONL 重新统计 Token，未变化日志只原子替换范围内派生数据，变化日志仍完整同步以免跳过新事件

### v0.2.26(20260723)
#### feature:
1. 对相同扫描目标的连续全量 Token 快照检测归零和物质性翻倍，并在统计页与状态栏弹框提示核对，目标集合变化和小额自然增长不误报

### v0.2.25(20260723)
#### feature:
1. 在统计页和状态栏按会话展示 Token 覆盖率，并将未统计会话拆分为未发现日志、无 Token 事件、日志待更新和扫描失败，避免子代理日志放大原因数量

### v0.2.24(20260723)
#### bugFix:
1. 本地时区变化后自动重建 Token 日汇总并使统计快照失效，避免今天、昨天和每日趋势继续沿用旧时区边界

### v0.2.23(20260723)
#### quality:
1. 补齐 Token 日志追加、截断、同路径替换和读取失败的端到端回归矩阵，固定增量续扫、全量重建、SQLite 汇总替换和统计覆盖降级行为

### v0.2.22(20260723)
#### bugFix:
1. 显式识别并幂等跳过重复的累计 Token 检查点，在扫描诊断中展示本轮去重数量，避免重复事件污染日汇总和额度周期明细

### v0.2.21(20260723)
#### feature:
1. 每轮扫描最多用一个到期的原始 JSONL 全量重建 Token 数据库，自动修复文件未变化时的静默错账；成功后 24 小时内继续复用缓存

### v0.2.20(20260723)
#### optimization:
1. 统一总 Token、非缓存 Token 与额度消耗口径：总量采用本地日志且已包含缓存输入，非缓存量按总量减缓存输入计算，服务端额度百分比不再与本地 Token 混称或换算

### v0.2.19(20260723)
#### optimization:
1. 侧栏输入未变化时复用项目目录树与分类计数快照，避免界面状态更新重复扫描全部活跃会话

### v0.2.18(20260723)
#### optimization:
1. 会话数据与筛选条件未变化时复用筛选排序结果，并在时间窗口跨过会话边界时准确重建

### v0.2.17(20260723)
#### optimization:
1. 会话线程、元数据、标签和项目归属未变化时复用展示快照，避免无关状态刷新重复装配列表数据

### v0.2.16(20260723)
#### optimization:
1. 会话列表每次筛选仅构建一次标签索引，各会话按关联标签直接查找，避免重复扫描全部标签

### v0.2.15(20260723)
#### optimization:
1. 侧栏一次遍历生成收藏、未分类、无项目、分类与标签计数，避免分类和标签越多时重复扫描全部会话

#### bugFix:
1. Token 日志仅追加且扫描仍在追赶时保留已解析历史用量，同时继续标记数据过期；截断、替换或读取失败仍排除统计

### v0.2.14(20260723)
#### optimization:
1. 状态栏、弹窗与主窗口按时间范围复用统计快照，仅在相关数据或本地日期变化时重新聚合

### v0.2.13(20260723)
#### optimization:
1. 额度与统计页不再为状态更新构建、筛选和排序隐藏的会话列表，降低默认页面渲染开销

### v0.2.12(20260723)
#### optimization:
1. 归档或取消归档成功后直接更新本地会话列表，不再为单条状态变化重新加载全部会话并启动 Token 扫描

### v0.2.11(20260723)
#### optimization:
1. 会话列表单次渲染复用同一份筛选排序结果，避免列表、空状态和选择同步反复构建全部可见会话

### v0.2.10(20260723)
#### optimization:
1. 项目目录树改为按路径祖先建立父子索引，避免会话较多时在主线程反复两两比较全部项目路径

### v0.2.9(20260723)
#### feature:
1. 新增持久化 Token 日志发现索引，复用未变化文件的元数据和父子代理归属，减少重复读取

#### optimization:
1. 秒级 Token 明细仅保留最近 30 天，长期历史继续保留完整日汇总，控制数据库后续增长
2. macOS 低电量模式下将后台额度快照有效期从 10 分钟放宽到 30 分钟，并将自动完整会话扫描限制为最多每小时一次，手动刷新不受影响
3. 补充发现缓存命中、实际元数据读取、读取字节和失败数量等 Token 扫描诊断

#### bugFix:
1. 修复无法归属的日志读取失败时仍可能把少算的 Token 结果显示为完整覆盖的问题

### v0.2.8(20260723)
#### feature:
1. 状态栏安全截图默认隐藏账号邮箱，并保留右键复制完整截图的显式入口

#### optimization:
1. 在状态栏弹窗和主窗口展示 Token 扫描健康状态，区分最新、过期和失败数据
2. 统一 App、Codex App Server 握手和发布记录中的版本信息

#### bugFix:
1. 修复父会话与子代理日志重复、未完整扫描或空缓存增长时导致的 Token 重复统计
2. 修复过期或扫描失败的 Token 缓存仍进入当前额度周期统计的问题
3. 修复截图可能泄露账号邮箱的问题，同时继续保持当前深浅色主题

### v0.2.7(20260721)
#### feature:
1. 新增状态栏完整内容一键截图，生成 PNG 后直接写入剪贴板，支持粘贴到聊天或文档

#### bugFix:
1. 修复截图未完整包含滚动区域、截图失败后残留旧剪贴板内容，以及跟随系统暗色时错误生成亮色截图的问题

### v0.2.6(20260721)
#### feature:
1. 状态栏弹框按本周额度周期展示每日 Token 消耗，并继续以 Codex 服务端数据展示周额度和重置时间

#### optimization:
1. 保持 10 分钟低能耗刷新策略，不增加轮询和数据库写入

#### bugFix:
1. 使用稳定的原生柱状图，修复此前弹框崩溃风险

### v0.2.5(20260721)
#### feature:
1. 新增“启动时默认打开主窗口”设置，默认关闭，开启后下次启动自动显示主窗口和 Dock 图标

#### bugFix:
1. Token 统计纳入子代理实际用量，并排除子代理日志中复制的父会话历史，避免遗漏或重复计算

### v0.2.4(20260720)
#### feature:
1. 新增版本更新检查，默认每 24 小时最多请求一次 GitHub Release，并支持在菜单栏弹窗查看更新、忽略版本和手动检查；应用不会自动下载或安装更新

#### optimization:
1. 统一菜单栏弹窗刷新按钮与其他顶部按钮的视觉状态，刷新期间保持正常颜色并通过旋转图标提示进度

### v0.2.3(20260720)
#### optimization:
1. 调整为默认仅在 macOS 菜单栏运行，不主动打开主窗口或显示 Dock 图标；从菜单栏弹窗打开主窗口时显示 Dock，关闭主窗口后继续在后台运行

### v0.2.2(20260719)
#### feature:
1. 新增主窗口“额度”一级页面，集中展示 Codex 账户、每周额度、本额度周期本地 Token、额度重置卡次数及逐张到期详情，避免菜单栏入口被系统隐藏时无法查看

#### optimization:
1. 完善额度轻量刷新状态，支持页面打开时按过期策略刷新、手动刷新、最后成功时间和失败提示，并在失败时继续保留上一次成功快照
2. 补充每周额度的本地时区准确重置时间，同时明确服务端额度与本地 Token 统计不能直接换算

### v0.2.1(20260718)
#### bugFix:
1. 修复 Codex linked worktree 按各自绝对路径重复显示同名项目的问题，改为依据 Git common directory 合并到主工作树，并在远端仓库身份唯一时归并已删除 worktree 的历史会话

### v0.2.0(20260718)
#### feature:
1. 新增 Codex 临时会话工作区自动识别，严格匹配 `Codex/YYYY-MM-DD/<会话目录>` 路径，并提供“无项目”侧栏入口集中查看未关联项目的会话
2. 支持从临时会话工作区内的 Git 仓库和会话证据识别真实项目，识别成功时仅展示真实项目，识别不足或候选冲突时归为“无项目”
3. 新增额度重置卡信息展示，可在菜单栏弹窗查看当前可用次数、最近到期时间和逐张到期详情
4. 新增 Token 趋势选点交互，支持悬停预览、单击固定日期，并查看输入、缓存输入、输出、推理输出及总 Token 明细

#### optimization:
1. 重构项目归属缓存模型，明确区分真实项目、普通工作目录和无项目状态，并通过分类器版本触发活跃及归档会话的后台重算
2. 完善项目缓存 SQLite 增量迁移，保留收藏、分类、标签和 Token 缓存，避免要求用户删除本地数据库
3. 调整 Token 统计口径，无项目会话继续计入总量、趋势和会话排行，但不进入项目 Token 排行
4. 修改原“未分类”侧栏文案为“未加入分类”，明确其仍表示尚未加入自定义分类
5. 抽取主窗口与菜单栏共用的 Token 趋势组件，统一点位选择、单点标记、紧凑数字和辅助功能语义
6. 在打开菜单栏弹窗时轻量刷新配额与重置卡信息，并保留原有定时刷新和失败时的上次成功快照
7. 保留并继续写入 v0.1.2 的原每日统计表，以增量新表保存精确 Token 数据，支持旧数据库原位升级和旧版本忽略新表后继续运行

#### bugFix:
1. 修复 Codex 临时会话目录在无法识别 Git 项目时回退为原始 `cwd`，导致大量一次性目录被计入项目树和项目排行的问题
2. 修复同一天使用额度重置卡后“已用 Token”仍包含重置前用量的问题，改为按服务端额度周期的秒级起点统计本地 Token

### v0.1.2(20260717)
#### optimization:
1. 优化菜单栏弹窗的每日 Token 趋势图，在右侧新增纵轴刻度，并使用中文紧凑数字格式展示 Token 数量

### v0.1.1(20260716)
#### optimization:
1. 优化菜单栏弹窗顶部操作区，将官网、GitHub、刷新和打开主窗口统一为带悬停反馈的圆形图标按钮，并增加分隔线改善操作层次
2. 调整官网与 GitHub 的入口图标，使用 Safari 系统图标和 GitHub 标识提升辨识度
3. 调整菜单栏周配额圆环线宽，增强剩余配额在紧凑状态栏中的可读性

### v0.1.0(20260716)
#### feature:
1. 新增原生 macOS Codex 会话管理界面，支持搜索、排序、时间筛选、归档、取消归档和从 Codex 重新打开会话
2. 新增项目目录推断、收藏、自定义分类和彩色标签能力，支持按项目和自定义元数据组织本机会话
3. 新增本地 Token 用量统计，支持按日期、项目和会话查看输入、缓存输入、输出、推理输出及总 Token
4. 新增菜单栏状态与弹窗，支持查看会话数量、Token 覆盖率、周配额剩余比例和当前配额周期 Token 用量
5. 新增 Token 趋势图与项目排行，支持单日点位、多日折线和项目 Token 对比
6. 新增系统、浅色和深色主题切换，支持应用外观跟随 macOS 系统设置
7. 新增本地 SQLite 元数据存储与旧版 `Codex Sessions` 数据库迁移，保留原数据库且不修改 Codex 会话内容

#### note:
1. 明确 SessionNest 采用本机优先架构，不包含遥测、云同步或独立账号系统，收藏、分类、标签和统计结果均保留在当前 Mac

#### script:
1. 新增严格 Swift 格式检查、串行测试、GitHub Actions 持续集成和本地 App 打包签名脚本，支持源码构建与发布前验证
